<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <com.handmark.pulltorefresh.library.PullToRefreshListView
        android:id="@+id/lv_record_mainlist"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:divider="@color/grey215"
        android:listSelector="@android:color/transparent"
        android:scrollbars="none" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@drawable/common_top_line_bg">
        <Button
            android:id="@+id/tv_record_mainlist_toupload"
            style="@style/orangeButton"
            android:layout_marginBottom="5dp"
            android:layout_marginLeft="@dimen/record_uploadbutton_sidemargin"
            android:layout_marginRight="@dimen/record_uploadbutton_sidemargin"
            android:layout_marginTop="5dp"
            android:text="@string/record_doupload" />
    </LinearLayout>


</LinearLayout>